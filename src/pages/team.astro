---
import BaseLayout from '../layouts/BaseLayout.astro';
import TeamMember from '../components/TeamMember.astro';
import teamData from '../data/team.json';
import siteData from '../data/team-site.json';

const teamMembers = teamData.members;
const h = siteData.hero;
const belief = siteData.belief;
const aia = siteData.aia;
const stats = siteData.stats;
const founder = siteData.founder;
const recognition = siteData.recognition;
const beBest = siteData.beBest;
const entrepreneur = siteData.entrepreneur;
const recruitment = siteData.recruitment;
const cta = siteData.cta;
const maxSource = Math.max(...recruitment.sourceChart.items.map(i => parseFloat(i.value)));
const anchors = [
  ['#vision','愿景与优势'],['#aia','友邦百年'],['#founder','创始人'],['#people','团队介绍'],
  ['#plans','人才计划'],['#entrepreneur','企业家计划'],['#data','招募数据'],['#join','加入我们'],
];
const heroHtml = h.headline.replace(h.headlineAccent, `<em>${h.headlineAccent}</em>`);
const sloganHtml = h.slogan.replace('可乐', '<em>可乐</em>');
---
<BaseLayout title="可乐团队｜专业保险顾问团队" description="了解未来可乐团队的长期主义理念、跨行业专业背景、创始人经历、人才培养计划与 2026 上半年招募数据。">
  <section class="team-hero" id="vision"><div class="container hero-grid">
    <div class="hero-main">
      <p class="eyebrow on-dark">{h.eyebrow}</p>
      <h1 set:html={heroHtml} />
      <p class="hero-sub">{h.subheadline}</p>
      <div class="hero-slogan"><b set:html={sloganHtml} /><span>{h.sloganEn}</span></div>
      <div class="hero-tags">{h.tags.map(tag => <span>{tag}</span>)}</div>
    </div>
    <div class="hero-media">
      <img src={h.image} loading="lazy" alt="未来可乐团队" />
    </div>
  </div></section>

  <nav class="chapter-nav" aria-label="页面章节"><div class="container">{anchors.map(([id,label]) => <a href={id}>{label}</a>)}</div></nav>

  <section class="numbers"><div class="container number-grid">{stats.map(s => <article><b data-count={s.value} >{s.value}</b><span>{s.label}</span><small>{s.note}</small></article>)}</div></section>

  <section class="section aia reveal" id="aia"><div class="container">
    <header class="aia-head"><div><p class="eyebrow">{aia.eyebrow}</p><h2 class="section-title">{aia.title}</h2></div><p class="aia-sub">{aia.subtitle}</p></header>
    <div class="aia-track-wrap">
      <div class="aia-track" id="aiaTrack">
        {[...aia.milestones, ...aia.milestones].map((m, i) => <article class:list={['aia-slide', { featured: m.featured }]}>
          <div class="aia-media"><img src={m.image} loading="lazy" width="1280" height="800" alt={`${m.year}，${m.title}`} /><span class="aia-year">{m.year}</span>{m.featured && <span class="aia-featured">我们的起点</span>}</div>
          <div class="aia-body"><b>{m.year}</b><h3>{m.title}</h3><p>{m.desc}</p></div>
        </article>)}
      </div>
    </div>
    <div class="aia-progress" aria-hidden="true"><div class="aia-progress-bar" id="aiaProgressBar"></div></div>
    <p class="aia-note">{aia.note}</p>
  </div></section>

  <section class="section belief reveal"><div class="container belief-grid"><div><p class="eyebrow">{belief.eyebrow}</p><h2 class="section-title" set:html={belief.title.split(' · ').map((s,i) => `<span class="title-seg">${s}</span>${i<2?' · ':''}`).join('')} /></div><div>{belief.paragraphs.map(p => <p>{p}</p>)}</div></div></section>

  <section class="section founder-band reveal" id="founder"><div class="container founder-grid">
    <div class="founder-media">
      <img src={founder.image} loading="lazy" width="817" height="1090" alt={`${founder.name}，未来可乐团队创始人`} />
    </div>
    <div><div class="founder-name-row"><h2 class="section-title">{founder.name}</h2><img class="founder-logo" src="/assets/logo.png" alt="" aria-hidden="true" /></div><p class="founder-meta">{founder.school} · {founder.years} · {founder.role}</p><p class="founder-tagline">{founder.tagline}</p>
    {founder.paragraphs.map(p => <p class="founder-p" set:html={p.replace(/“可乐”/g, '<em class="cola-em">“可乐”</em>')} />)}
    <div class="founder-highlights">{founder.highlights.map(item => <div class="fh-card"><span>{item.label}</span><b class:list={['fh-value',{ big:item.value === '10年+' }]}>{item.value}</b></div>)}</div>
    </div>
  </div></section>

  <section class="section members reveal" id="people"><div class="container">
    <header class="members-head"><p class="eyebrow">{siteData.members.eyebrow}</p><h2 class="section-title">{siteData.members.title}</h2><p class="section-lead">{siteData.members.lead}</p></header>
    <div class="members-grid">{teamMembers.map(member => <TeamMember {...member} />)}</div>
    <a class="vacancy" href={cta.buttonUrl} target="_blank" rel="noopener noreferrer">
      <span class="vacancy-plus">+</span>
      <span class="vacancy-copy"><b>{siteData.members.vacancyTitle}</b><small>{siteData.members.vacancySub}</small></span>
      <span class="vacancy-arrow">立即申请</span>
    </a>
  </div></section>

  <section class="section recognition reveal"><div class="container recognition-grid"><div><p class="eyebrow">{recognition.eyebrow}</p><h2 class="section-title">{recognition.title}</h2><p class="section-lead">{recognition.lead}</p></div><div class="recognition-list">{recognition.items.map(item => <span>{item}</span>)}</div></div></section>

  <section class="section bebest reveal" id="plans"><div class="container">
    <p class="eyebrow">{beBest.eyebrow}</p><h2 class="section-title">{beBest.title}</h2><p class="section-lead">{beBest.subtitle}</p>
    <p class="plan-badge">基础人才计划 · 面向更广泛人群</p>
    <div class="bebest-grid">{beBest.cards.map(card => <article class="bebest-card"><span class="period">{card.period}</span><b>{card.amount}<em>{card.unit}</em></b><p>{card.desc}</p></article>)}</div>
    <div class="plan-conditions"><h3>招募条件</h3><ul>{beBest.conditions.map(c => <li>{c}</li>)}</ul></div>
    <p class="plan-note">{beBest.note}</p>
  </div></section>

  <section class="section entrepreneur reveal" id="entrepreneur"><div class="container">
    <p class="eyebrow on-dark">{entrepreneur.eyebrow}</p><h2 class="section-title">{entrepreneur.title}</h2><p class="section-lead light-lead">{entrepreneur.subtitle}</p>
    <p class="plan-badge light-badge">精英人才计划 · 面向跨行业精英 · 三档定级</p>
    <div class="entrepreneur-grid">{entrepreneur.plans.map(plan => <article class:list={['e-plan',{ featured:plan.featured }]}>
      <header><h3>{plan.name}</h3><span>{plan.subname}</span></header>
      <div class="stipend"><span>专业提升期津贴</span><b>{plan.stipend}<em>{plan.unit}</em></b></div>
      <ul class="targets"><li>首三月创无忧收入可达 <strong>{plan.first3m}</strong></li><li>首十二月目标收入可达 <strong>{plan.first12m}</strong></li></ul>
      <div class="e-conditions"><h4>招募条件</h4><ul>{plan.conditions.map(c => <li>{c}</li>)}</ul></div>
    </article>)}</div>
    {entrepreneur.support && <div class="support-panel"><h3>{entrepreneur.support.title}</h3><div class="support-grid">{entrepreneur.support.blocks.map(block => <div class="support-block"><h4>{block.title}</h4><ul>{block.lines.map(l => <li>{l}</li>)}</ul></div>)}</div><p class="plan-note">{entrepreneur.support.note}</p></div>}
  </div></section>

  <section class="section recruitment reveal" id="data"><div class="container">
    <p class="eyebrow">{recruitment.eyebrow}</p><h2 class="section-title">{recruitment.title}</h2><p class="section-lead">{recruitment.lead}</p>
    <div class="r-hero-stats">
      <div class="r-stat"><b data-count={recruitment.applied} >{recruitment.applied}</b><span>入职申请</span></div>
      <div class="r-stat brand"><b data-count={recruitment.joined} >{recruitment.joined}</b><span>成功入职</span></div>
      <div class="r-stat"><b data-count={recruitment.acceptance} >{recruitment.acceptance}</b><span>录用率</span></div>
    </div>
    <div class="r-grid">
      <article class="r-card"><div class="r-card-head"><h3>{recruitment.education.title}</h3><div class="r-big"><span data-count={recruitment.education.big} >{recruitment.education.big}</span><em>{recruitment.education.bigLabel}</em></div></div><ul>{recruitment.education.points.map(p => <li>{p}</li>)}</ul></article>
      <article class="r-card"><div class="r-card-head"><h3>{recruitment.transformation.title}</h3><div class="r-big"><span data-count={recruitment.transformation.big} >{recruitment.transformation.big}</span><em>{recruitment.transformation.bigLabel}</em></div></div><div class="r-grid2">{recruitment.transformation.grid.map(g => <div class="r-cell"><span>{g.label}</span><b data-count={g.value} >{g.value}</b></div>)}</div></article>
    </div>
    <div class="r-young"><div class="ry-left"><h3>{recruitment.young.title}</h3><p>{recruitment.young.subtitle}</p><div class="ry-bands">{recruitment.young.ageBands.map(band => <div class="ry-band"><span>{band.label}</span><b data-count={band.count} >{band.count}</b><em>{band.percent}</em></div>)}</div></div><div class="ry-right">{recruitment.young.metrics.map(m => <div class="ry-metric"><b data-count={m.value} >{m.value}</b><span>{m.label}</span></div>)}</div></div>
    <div class="r-source"><div class="rs-head"><h3>{recruitment.sourceChart.title}</h3><p>{recruitment.sourceChart.subtitle}</p></div><div class="rs-bars">{recruitment.sourceChart.items.map(item => <div class="rs-bar" style={`--w:${(parseFloat(item.value)/maxSource)*100}%`}><span class="rs-label">{item.label}</span><div class="rs-track"><div class="rs-fill"></div></div><span class="rs-value">{item.value}</span></div>)}</div></div>
  </div></section>

  <section class="section join" id="join"><div class="container">
    <div class="join-card reveal">
      <p class="eyebrow on-dark">{cta.eyebrow}</p>
      <h2 set:html={cta.title} />
      <p class="join-text">{cta.text}</p>
      <div class="join-steps">{cta.steps.map(step => <div class="join-step"><b>{step.num}</b><span>{step.title}</span><small>{step.desc}</small></div>)}</div>
      <a class="button join-btn" href={cta.buttonUrl} target="_blank" rel="noopener noreferrer">{cta.buttonText}<span class="btn-arrow">→</span></a>
    </div>
  </div></section>
  <a class="join-fab" href={cta.buttonUrl} target="_blank" rel="noopener noreferrer">{cta.buttonText}</a>
</BaseLayout>
<script>
  const io = new IntersectionObserver((entries) => {
    entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('in'); io.unobserve(e.target); } });
  }, { threshold: 0.15 });
  document.querySelectorAll('.reveal').forEach(el => io.observe(el));

  const counters = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (!e.isIntersecting) return;
      const el = e.target;
      const raw = el.dataset.count || '';
      const match = raw.match(/^([\d.]+)(.*)$/);
      if (!match) return;
      const target = parseFloat(match[1]);
      const suffix = match[2];
      const dur = 1500;
      const start = performance.now();
      const tick = (now) => {
        const p = Math.min((now - start) / dur, 1);
        const eased = 1 - Math.pow(1 - p, 3);
        el.textContent = Math.round(target * eased) + suffix;
        if (p < 1) requestAnimationFrame(tick);
      };
      requestAnimationFrame(tick);
      counters.unobserve(el);
    });
  }, { threshold: 0.5 });
  document.querySelectorAll('[data-count]').forEach(el => counters.observe(el));

  const bars = new IntersectionObserver((entries) => {
    entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('in'); bars.unobserve(e.target); } });
  }, { threshold: 0.4 });
  document.querySelectorAll('.rs-bar').forEach(el => bars.observe(el));

  const fab = document.querySelector('.join-fab');
  const joinEl = document.querySelector('#join');
  window.addEventListener('scroll', () => {
    if (!fab || !joinEl) return;
    const jr = joinEl.getBoundingClientRect();
    fab.classList.toggle('show', window.scrollY > 500 && jr.top > window.innerHeight);
  }, { passive: true });

  const aiaWrap = document.querySelector('.aia-track-wrap');
  if (aiaWrap && !window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    let paused = false;
    const speed = 0.7;
    const step = () => {
      if (!paused) {
        const slides = aiaWrap.querySelectorAll('.aia-slide');
        const max = aiaWrap.scrollWidth - aiaWrap.clientWidth;
        if (slides.length > 1 && max > 0) {
          const half = slides[slides.length / 2].offsetLeft;
          aiaWrap.scrollLeft += speed;
          if (aiaWrap.scrollLeft >= half) aiaWrap.scrollLeft -= half;
        }
      }
      requestAnimationFrame(step);
    };
    requestAnimationFrame(step);
    aiaWrap.addEventListener('mouseenter', () => { paused = true; }, { passive: true });
    aiaWrap.addEventListener('mouseleave', () => { paused = false; }, { passive: true });
    aiaWrap.addEventListener('touchstart', () => { paused = true; }, { passive: true });
    aiaWrap.addEventListener('touchend', () => { paused = false; }, { passive: true });
  }
</script>
<style>
  .team-hero { min-height: 620px; display:grid; align-items:end; padding: 130px 0 96px; background: linear-gradient(120deg,rgba(51,61,71,.97),rgba(51,61,71,.8)),url('/assets/images/hero-bg.jpg') center/cover; color:white; }
  .hero-grid { display:grid; grid-template-columns: 1.25fr .9fr; gap: clamp(32px, 6vw, 84px); align-items:end; }
  .team-hero h1 { margin:0; font-size:clamp(2.8rem,6vw,5.2rem); line-height:1.06; letter-spacing:-.04em; font-weight:900; text-wrap:balance; }
  .team-hero h1 :global(em) { font-style:normal; color:var(--color-olive); display:inline-block; }
  .team-hero h1:after { content:''; display:block; width:56px; height:5px; margin:26px 0 0; background:var(--color-olive); }
  .eyebrow.on-dark { color:var(--color-olive); }
  .hero-sub { margin:26px 0 0; max-width:560px; color:rgba(255,255,255,.85); font-size:clamp(1rem,1.8vw,1.22rem); line-height:1.75; text-wrap:balance; }
  .hero-slogan { margin:34px 0 0; padding-top:26px; border-top:1px solid rgba(255,255,255,.22); display:grid; gap:6px; }
  .hero-slogan b { font-size:1.3rem; letter-spacing:.12em; font-weight:700; white-space:nowrap; }
  .hero-slogan b :global(em) { font-style:normal; color:var(--color-olive); font-weight:900; }
  .hero-slogan span { color:rgba(255,255,255,.62); font-size:.8rem; letter-spacing:.14em; text-transform:uppercase; }
  .hero-tags { display:flex; flex-wrap:wrap; gap:8px; margin-top:28px; }.hero-tags span { padding:8px 14px; border:1px solid rgba(255,255,255,.28); border-radius:999px; font-size:.82rem; }
  .hero-media { position:relative; }
  .hero-media:before { content:''; position:absolute; inset:16px -16px -16px 16px; border:2px solid rgba(165,145,92,.5); border-radius:var(--radius-lg); pointer-events:none; }
  .hero-media img { position:relative; width:100%; aspect-ratio: 4/3; object-fit:cover; border-radius:var(--radius-lg); box-shadow:0 30px 80px rgba(0,0,0,.45); }
  .chapter-nav { position:sticky; z-index:30; top:64px; border-top:1px solid var(--color-line); border-bottom:1px solid var(--color-line); background:rgba(255,255,255,.94); backdrop-filter:blur(16px); }
  .chapter-nav .container { display:flex; gap:8px; overflow-x:auto; padding-top:10px; padding-bottom:10px; }
  .chapter-nav a { flex:0 0 auto; padding:8px 13px; border-radius:999px; color:var(--color-muted); font-size:.78rem; font-weight:750; text-decoration:none; }
  .chapter-nav a:hover { background:rgba(165,145,92,.13); color:var(--color-ink); }
  .numbers { background:var(--color-olive); color:white; }.number-grid { display:grid; grid-template-columns:repeat(4,1fr); }.number-grid article { display:grid; padding:34px 5%; border-right:1px solid rgba(255,255,255,.25); }.number-grid article:last-child{border-right:none}.number-grid b{font-size:2rem;letter-spacing:-.02em}.number-grid span{opacity:.94;font-weight:700}.number-grid small{opacity:.62;font-size:.78rem}
  .belief { background:white; }.belief-grid { display:grid; grid-template-columns:1fr 1fr; gap:8vw; align-items:center; }.belief-grid>div:last-child { color:var(--color-muted); font-size:1.08rem; }.belief-grid p+p{margin-top:1.2em}.title-seg{white-space:nowrap;display:inline-block}:global(.title-seg){white-space:nowrap;display:inline-block}
  .aia { background:var(--color-canvas); }.aia-head { display:grid; grid-template-columns:1.2fr 1fr; gap:6vw; align-items:end; }.aia-sub { margin:0; color:var(--color-muted); font-size:1.02rem; line-height:1.85; text-wrap:balance; }
  .aia-track-wrap { margin-top:52px; overflow-x:auto; scrollbar-width:thin; scrollbar-color:rgba(165,145,92,.5) transparent; scrollbar-gutter:stable; }
  .aia-track { display:flex; gap:26px; width:max-content; padding-bottom:8px; }
  .aia-slide { flex:0 0 min(400px, 82vw); overflow:hidden; border:1px solid var(--color-line); border-radius:var(--radius-md); background:white; transition:transform .35s, box-shadow .35s; }
  .aia-slide:hover { transform:translateY(-6px); box-shadow:var(--shadow-card); }
  .aia-media { position:relative; aspect-ratio:16/10; overflow:hidden; background:var(--color-charcoal); }
  .aia-media img { width:100%; height:100%; object-fit:cover; transition:transform .7s cubic-bezier(.2,.7,.3,1); }
  .aia-slide:hover .aia-media img { transform:scale(1.05); }
  .aia-year { position:absolute; top:16px; left:16px; padding:8px 16px; border-radius:999px; background:rgba(51,61,71,.82); color:white; font-size:1.15rem; font-weight:800; letter-spacing:.04em; backdrop-filter:blur(6px); }
  .aia-featured { position:absolute; top:16px; right:16px; padding:8px 16px; border-radius:999px; background:var(--color-brand); color:white; font-size:.82rem; font-weight:800; letter-spacing:.06em; backdrop-filter:blur(6px); }
  .aia-body { padding:26px 26px 30px; }
  .aia-body b { display:block; color:var(--color-brand); font-size:2.6rem; line-height:1; letter-spacing:-.02em; }
  .aia-body h3 { margin:10px 0 8px; font-size:1.28rem; }
  .aia-body p { margin:0; color:var(--color-muted); font-size:.92rem; line-height:1.75; text-wrap:balance; }
  .aia-slide.featured { border-color:var(--color-brand); box-shadow:0 0 0 2px rgba(211,17,69,.14), var(--shadow-card); }
  .aia-progress { margin-top:26px; height:3px; border-radius:999px; background:rgba(51,61,71,.12); overflow:hidden; }
  .aia-progress-bar { height:100%; width:0; background:linear-gradient(90deg,var(--color-olive),var(--color-brand)); }
  .aia-note { margin:14px 0 0; color:var(--color-muted); font-size:.76rem; text-align:center; }
  .founder-band { background:var(--color-canvas); }.founder-grid { display:grid; grid-template-columns:.7fr 1.3fr; gap:6vw; align-items:start; }.founder-media { position:relative; }.founder-media:before { content:''; position:absolute; top:22px; left:-22px; width:100%; height:100%; border:2px solid rgba(165,145,92,.55); border-radius:var(--radius-md); }  .founder-media img { position:relative; max-height:540px; width:100%; object-fit:contain; object-position:top; border-radius:var(--radius-md); background:white; border:1px solid var(--color-line); box-shadow:var(--shadow-card); }
  .founder-name-row { position:relative; }
  .founder-name-row .founder-logo { position:absolute; z-index:2; top:50%; right:0; transform:translateY(-50%); width:140px; opacity:.1; pointer-events:none; }
  .founder-meta { margin:10px 0 0; color:var(--color-olive); font-weight:750; font-size:.9rem; }
  .founder-tagline { margin:18px 0 0; font-size:1.25rem; font-weight:700; line-height:1.5; color:var(--color-charcoal); }
  .founder-p { margin:14px 0 0; color:var(--color-muted); line-height:1.9; }
  .founder-p :global(.cola-em) { font-style:normal; color:var(--color-olive); font-weight:800; font-size:1.12em; letter-spacing:.02em; }
  .founder-highlights { display:flex; gap:14px; margin-top:26px; }.fh-card { flex:1; padding:18px 20px; background:white; border:1px solid var(--color-line); border-radius:var(--radius-md); }.fh-card span{display:block;color:var(--color-muted);font-size:.78rem;font-weight:700}.fh-card b{display:block;margin-top:6px;font-size:1.05rem;line-height:1.5;color:var(--color-charcoal)}.fh-card b.fh-value.big{font-size:2.6rem;font-weight:900;color:var(--color-olive);line-height:1}
  .members { background:white; }.members-head { max-width:680px; }.members-grid { display:grid; grid-template-columns:repeat(5,1fr); gap:22px; margin-top:52px; }
  .vacancy { margin-top:26px; display:grid; grid-template-columns:auto 1fr auto; gap:22px; align-items:center; padding:26px 30px; border:2px dashed rgba(165,145,92,.6); border-radius:var(--radius-md); background:rgba(165,145,92,.05); text-decoration:none; transition:background .3s, transform .3s; }
  .vacancy:hover { background:rgba(165,145,92,.12); transform:translateY(-2px); }
  .vacancy-plus { display:grid; place-items:center; width:52px; height:52px; border-radius:50%; background:var(--color-olive); color:white; font-size:1.8rem; font-weight:300; }
  .vacancy-copy b { display:block; font-size:1.3rem; color:var(--color-charcoal); }.vacancy-copy small { color:var(--color-muted); font-size:.9rem; }
  .vacancy-arrow { padding:12px 22px; border-radius:999px; background:var(--color-brand); color:white; font-weight:750; font-size:.9rem; white-space:nowrap; }
  .recognition { background:var(--color-canvas); }.recognition-grid { display:grid; grid-template-columns:1fr 1fr; gap:8vw; align-items:center; }.recognition-list { display:grid; grid-template-columns:1fr 1fr; gap:12px; }.recognition-list span { padding:26px 20px; border-left:3px solid var(--color-olive); background:white; font-weight:750; }
  .bebest { background:white; }.plan-badge { display:inline-block; margin:18px 0 0; padding:7px 14px; border-radius:999px; background:rgba(165,145,92,.14); color:var(--color-dark-green); font-size:.78rem; font-weight:800; }
  .bebest-grid { display:grid; grid-template-columns:repeat(3,1fr); gap:18px; margin-top:36px; }.bebest-card { padding:30px; border:1px solid var(--color-line); border-radius:var(--radius-md); background:var(--color-canvas); transition:transform .35s, box-shadow .35s, border-color .35s; }.bebest-card:hover { transform:translateY(-4px); box-shadow:var(--shadow-card); border-color:rgba(211,17,69,.35); }.bebest-card .period { display:block; color:var(--color-brand); font-weight:800; font-size:.8rem; }.bebest-card b { display:block; margin:12px 0 8px; font-size:2.6rem; letter-spacing:-.02em; }.bebest-card b em { font-style:normal; font-size:1rem; color:var(--color-muted); }.bebest-card p { color:var(--color-muted); font-size:.9rem; }
  .plan-conditions { margin-top:30px; padding:22px 26px; border:1px solid var(--color-line); border-radius:var(--radius-md); }.plan-conditions h3{margin:0 0 10px;font-size:1rem}.plan-conditions ul { display:flex; flex-wrap:wrap; gap:10px; margin:0; padding:0; list-style:none; }.plan-conditions li { padding:7px 14px; border-radius:999px; background:rgba(211,17,69,.06); color:var(--color-brand); font-size:.82rem; font-weight:750; }
  .plan-note { margin:16px 0 0; color:var(--color-muted); font-size:.76rem; }
  .entrepreneur { background:var(--color-charcoal); color:white; }.light-lead { color:rgba(255,255,255,.7); }.light-badge { background:rgba(255,255,255,.1); color:var(--color-olive); }
  .entrepreneur-grid { display:grid; grid-template-columns:repeat(3,1fr); gap:18px; margin-top:36px; align-items:stretch; }
  .e-plan { display:flex; flex-direction:column; padding:26px; border:1px solid rgba(255,255,255,.14); border-radius:var(--radius-md); background:rgba(255,255,255,.04); transition:border-color .3s, background .3s; }
  .e-plan.featured { border-color:rgba(255,138,156,.5); background:rgba(255,255,255,.07); box-shadow:0 18px 50px rgba(0,0,0,.28); position:relative; }
  .e-plan.featured:before { content:'重点推荐'; position:absolute; top:14px; right:-1px; padding:5px 14px; background:var(--color-olive); color:white; font-size:.7rem; font-weight:800; border-radius:999px 0 0 999px; }
  .e-plan header h3 { margin:0; font-size:1.5rem; }.e-plan header span { color:var(--color-olive); font-size:.82rem; }
  .stipend { margin:22px 0; padding:18px 0; border-top:1px solid rgba(255,255,255,.12); border-bottom:1px solid rgba(255,255,255,.12); text-align:center; }.stipend span { display:block; color:rgba(255,255,255,.6); font-size:.76rem; }.stipend b { display:block; margin-top:6px; font-size:2.8rem; color:#ffb3bf; }.stipend b em { font-style:normal; font-size:1rem; color:rgba(255,255,255,.7); }
  .targets { margin:0 0 20px; padding:0; list-style:none; }.targets li { padding:6px 0; color:rgba(255,255,255,.85); font-size:.9rem; }.targets li strong { color:#ffb3bf; font-weight:800; }
  .e-conditions { padding:16px 18px; border-radius:var(--radius-md); background:rgba(255,255,255,.06); }.e-conditions h4 { margin:0 0 8px; font-size:.8rem; color:var(--color-olive); }.e-conditions ul { margin:0; padding:0; list-style:none; }.e-conditions li { padding:3px 0; font-size:.8rem; color:rgba(255,255,255,.78); }
  .support-panel { margin-top:34px; padding:34px; border:1px solid rgba(255,255,255,.14); border-radius:var(--radius-lg); background:rgba(255,255,255,.03); }.support-panel>h3 { margin:0 0 24px; text-align:center; font-size:1.3rem; }.support-grid { display:grid; grid-template-columns:1fr 1fr; gap:28px; }.support-block h4 { margin:0 0 12px; color:var(--color-olive); font-size:1rem; }.support-block ul { margin:0; padding:0; list-style:none; }.support-block li { padding:6px 0 6px 22px; position:relative; color:rgba(255,255,255,.85); font-size:.92rem; }.support-block li:before { content:''; position:absolute; left:4px; top:14px; width:6px; height:6px; border-radius:50%; background:var(--color-olive); }
  .entrepreneur .plan-note { color:rgba(255,255,255,.5); text-align:center; }
  .recruitment { background:white; }.r-hero-stats { display:grid; grid-template-columns:repeat(3,1fr); gap:16px; margin:34px 0; }.r-stat { padding:28px; text-align:center; border:1px solid var(--color-line); border-radius:var(--radius-md); background:var(--color-canvas); transition:transform .3s; }.r-stat:hover { transform:translateY(-3px); }.r-stat.brand { background:var(--color-brand); color:white; border-color:var(--color-brand); }.r-stat b { display:block; font-size:2.6rem; letter-spacing:-.02em; }.r-stat span { opacity:.8; font-size:.9rem; }
  .r-grid { display:grid; grid-template-columns:1fr 1fr; gap:18px; }.r-card { padding:28px; border:1px solid var(--color-line); border-radius:var(--radius-md); background:var(--color-canvas); }.r-card-head h3 { margin:0 0 16px; font-size:1.2rem; border-left:4px solid var(--color-brand); padding-left:14px; }.r-big { display:flex; align-items:baseline; gap:12px; margin-bottom:16px; }.r-big span { font-size:3.4rem; font-weight:800; color:var(--color-brand); line-height:1; }.r-big em { font-style:normal; font-size:.9rem; font-weight:600; color:var(--color-muted); }.r-card ul { margin:0; padding:0; list-style:none; }.r-card ul li { padding:7px 0; border-top:1px solid rgba(51,61,71,.09); color:var(--color-muted); font-size:.88rem; }
  .r-grid2 { display:grid; grid-template-columns:1fr 1fr; gap:10px; }.r-cell { padding:14px; border-radius:var(--radius-sm); background:white; text-align:center; }.r-cell span { display:block; color:var(--color-muted); font-size:.76rem; }.r-cell b { font-size:1.15rem; }
  .r-young { margin-top:18px; display:grid; grid-template-columns:1.4fr 1fr; gap:18px; }.ry-left { padding:28px; border-radius:var(--radius-md); background:var(--color-charcoal); color:white; }.ry-left h3 { margin:0 0 6px; font-size:1.2rem; }.ry-left p { margin:0 0 18px; color:rgba(255,255,255,.7); font-size:.9rem; }.ry-bands { display:grid; grid-template-columns:repeat(3,1fr); gap:10px; }.ry-band { padding:14px; text-align:center; border-radius:var(--radius-sm); background:rgba(255,255,255,.08); }.ry-band span { display:block; font-size:.76rem; color:rgba(255,255,255,.6); }.ry-band b { display:block; margin:4px 0; font-size:1.3rem; }.ry-band em { font-style:normal; font-size:.9rem; color:var(--color-olive); }
  .ry-right { display:grid; gap:10px; }.ry-metric { display:grid; place-items:center; padding:22px; border-radius:var(--radius-md); border:1px solid var(--color-line); background:var(--color-canvas); }.ry-metric b { font-size:2rem; color:var(--color-brand); }.ry-metric span { color:var(--color-muted); font-size:.82rem; }
  .r-source { margin-top:18px; padding:28px; border:1px solid var(--color-line); border-radius:var(--radius-md); }.rs-head h3 { margin:0; font-size:1.2rem; }.rs-head p { margin:4px 0 20px; color:var(--color-muted); font-size:.88rem; }.rs-bars { display:grid; gap:12px; }.rs-bar { display:grid; grid-template-columns:110px 1fr 60px; gap:14px; align-items:center; }.rs-label { font-size:.86rem; font-weight:700; }.rs-track { height:14px; border-radius:999px; background:rgba(51,61,71,.08); overflow:hidden; }.rs-fill { height:100%; width:0; border-radius:999px; background:linear-gradient(90deg,var(--color-brand),#ff5c80); transition:width 1.1s cubic-bezier(.2,.7,.3,1); }.rs-bar.in .rs-fill { width:var(--w); }.rs-value { font-size:.9rem; font-weight:800; color:var(--color-brand); }
  .join { background:var(--color-charcoal); color:white; position:relative; overflow:hidden; }
  .join:before { content:''; position:absolute; inset:0; background:linear-gradient(120deg,rgba(51,61,71,.92),rgba(51,61,71,.8)),url('/assets/images/hero-bg.jpg') center/cover; }
  .join-card { position:relative; z-index:1; max-width:880px; margin:0 auto; text-align:center; }
  .join h2 { margin:0; font-size:clamp(2rem,4.4vw,3.4rem); line-height:1.2; letter-spacing:-.03em; }
  .join-text { max-width:640px; margin:20px auto 0; color:rgba(255,255,255,.75); font-size:1.08rem; }
  .join-steps { display:grid; grid-template-columns:repeat(3,1fr); gap:18px; margin:44px 0 40px; }
  .join-step { padding:24px 18px; border:1px solid rgba(255,255,255,.16); border-radius:var(--radius-md); background:rgba(255,255,255,.05); }
  .join-step b { display:block; color:var(--color-olive); font-size:.9rem; letter-spacing:.14em; }
  .join-step span { display:block; margin:10px 0 6px; font-size:1.1rem; font-weight:750; }
  .join-step small { color:rgba(255,255,255,.6); font-size:.84rem; line-height:1.5; }
  .join-btn { padding:0 34px; min-height:56px; font-size:1.05rem; border-radius:999px; background:var(--color-brand); color:white; display:inline-flex; align-items:center; gap:10px; text-decoration:none; box-shadow:0 14px 40px rgba(211,17,69,.35); transition:transform .25s, box-shadow .25s, background .25s; }
  .join-btn:hover { background:var(--color-brand-dark); transform:translateY(-2px); box-shadow:0 20px 55px rgba(211,17,69,.45); }
  .btn-arrow { transition:transform .25s; }.join-btn:hover .btn-arrow { transform:translateX(5px); }
  .join-fab { position:fixed; right:22px; bottom:22px; z-index:60; padding:14px 24px; border-radius:999px; background:var(--color-brand); color:white; font-weight:800; text-decoration:none; box-shadow:0 14px 40px rgba(211,17,69,.4); opacity:0; transform:translateY(16px); pointer-events:none; transition:opacity .3s, transform .3s; }
  .join-fab.show { opacity:1; transform:none; pointer-events:auto; }
  .reveal { opacity:0; transform:translateY(26px); transition:opacity .7s ease, transform .7s cubic-bezier(.2,.7,.3,1); }
  .reveal.in { opacity:1; transform:none; }
  @media(prefers-reduced-motion: reduce) { .reveal { opacity:1; transform:none; transition:none; } .rs-fill{transition:none} }
  @media(max-width:1100px){.members-grid{grid-template-columns:repeat(4,1fr)}}
  @media(max-width:980px){.members-grid{grid-template-columns:repeat(3,1fr)}.entrepreneur-grid,.bebest-grid,.join-steps{grid-template-columns:1fr}.r-grid,.r-young,.support-grid{grid-template-columns:1fr}.join-steps{max-width:420px;margin-inline:auto}.hero-grid{grid-template-columns:1fr;align-items:start}.hero-media{max-width:560px;margin-top:6px}.hero-media:before{inset:12px -12px -12px 12px}}
  @media(max-width:720px){.belief-grid,.recognition-grid,.founder-grid{grid-template-columns:1fr}.number-grid{grid-template-columns:1fr 1fr}.members-grid{grid-template-columns:1fr 1fr}.join-card .button{justify-self:center}.founder-media img{max-height:420px}.chapter-nav{top:56px}.vacancy{grid-template-columns:auto 1fr}.vacancy-arrow{grid-column:1/-1;justify-self:start}.r-hero-stats{grid-template-columns:1fr}.founder-name-row .founder-logo{width:90px}.aia-head{grid-template-columns:1fr;gap:18px}.aia-slide{flex-basis:82vw}.aia-media{aspect-ratio:16/10}}
  @media(max-width:480px){.team-hero{min-height:auto;padding:96px 0 72px}.team-hero h1{font-size:2.6rem}.members-grid{grid-template-columns:1fr}.number-grid article{padding:24px 10px}.number-grid b{font-size:1.6rem}.number-grid span{font-size:.75rem}.rs-bar{grid-template-columns:90px 1fr 50px}}
</style>
